#
# Copyright The WildFly Authors
# SPDX-License-Identifier: Apache-2.0
#

mcp=The MCP subsystem
mcp.add=This operation adds the mcp subsystem
mcp.remove=This operation removes the mcp subsystem
mcp.sse-path=This is the context path for the SSE endpoint
mcp.messages-path=This is the context path for the messages endpoint
mcp.streamable-path=This is the context path for the streamable endpoint
mcp.page-size=Maximum number of items per paginated list response (tools, prompts, resources, resource templates). A value of 0 disables pagination and returns all items.
mcp.timeout=Idle connection timeout in seconds. Connections that have been inactive for longer than this value will be automatically closed.
mcp.request-state-secret=Base64-encoded HMAC secret (minimum 16 bytes decoded) used to sign requestState tokens for multi-round tool results. When set, enables MRTR support; tokens survive restarts and are valid across cluster members sharing the same secret.
mcp.allowed-origins=List of additional hostnames or origins allowed by DNS rebinding validation. Localhost addresses are always permitted. Use this to allow external hostnames that legitimately reach the server.
mcp.cache-ttl=Default cache TTL in milliseconds for list and discover responses. Defaults to 3600000 (1 hour). A value of 0 disables cache TTL emission.
mcp.cache-scope=Default cache scope for list and discover responses. Valid values are "public" and "private". Defaults to "public".

# Deployment model
mcp.deployment=MCP features deployed with this application
mcp.deployment.tool=An MCP tool exposed by this deployment
mcp.deployment.tool.description=Description of the MCP tool
mcp.deployment.tool.title=Human-readable title of the MCP tool
mcp.deployment.prompt=An MCP prompt exposed by this deployment
mcp.deployment.prompt.description=Description of the MCP prompt
mcp.deployment.resource=An MCP resource exposed by this deployment
mcp.deployment.resource.description=Description of the MCP resource
mcp.deployment.resource.mime-type=MIME type of the MCP resource content
mcp.deployment.resource.title=Human-readable title of the MCP resource
mcp.deployment.resource-template=An MCP resource template exposed by this deployment
mcp.deployment.resource-template.description=Description of the MCP resource template
mcp.deployment.resource-template.mime-type=MIME type of the MCP resource template content
mcp.deployment.resource-template.title=Human-readable title of the MCP resource template
